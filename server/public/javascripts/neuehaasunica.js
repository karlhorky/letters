FontFaceOnload( "NeueHaasUnica", {
  success: function() {
    var docEl = document.documentElement;

    // Stage 1 Complete
    // FOFT engaged
    docEl.className += " neuehaasunica-loaded";
  }
});

FontFaceOnload( "LibreCaslonDisplay", {
  success: function() {
    var docEl = document.documentElement;

    // Stage 1 Complete
    // FOFT engaged
    docEl.className += " librecaslon-loaded";
  }
});

FontFaceOnload( "AvenirNext", {
  success: function() {
    var docEl = document.documentElement;

    // Stage 1 Complete
    // FOFT engaged
    docEl.className += " avenirnext-loaded";
  }
});

FontFaceOnload( "AvenirNextBold", {
  success: function() {
    var docEl = document.documentElement;

    // Stage 1 Complete
    // FOFT engaged
    docEl.className += " avenirnext-bold-loaded";
  }
});
